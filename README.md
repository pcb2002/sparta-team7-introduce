
<img width="1189" height="907" alt="팀원 소개 페이지" src="https://github.com/user-attachments/assets/41b34b11-0ae1-4062-8fff-f8c3e9a21762" />

## 프로젝트 구조
```
sparta-team7-introduce/
├── .idea/             # 기본파일
├── image/             # 이미지 파일 디렉토리
├── members/           # 팀원 정보 JSON 파일 디렉토리
│   ├── 이니셜.json     # 팀원 json
│   ├── 이니셜.md       # 팀원 md
│   ├── members.json   # 팀원 목록 파일
│   └── example.json   # 예시 팀원 정보
├── web/               # 이미지 파일 디렉토리
│   ├── index.html     # 팀원 정보 html
│   ├── script.js      # 스크립트 js
└── └── style.css      # 스타일 css
```

## 협업 및 브랜치 관리 규칙 (Git Ruleset)

팀 프로젝트의 안정적인 코드 관리를 위해 아래와 같은 브랜치 보호 규칙(Branch Protection Rules)을 적용하고 있습니다.

### 1. 브랜치 구조
* **main**: 제품 배포를 위한 브랜치. 직접적인 `Push` 금지.
* **develop**: 기능을 합치고 테스트하는 공용 개발 브랜치.
* **feature/이름**: 각 기능별 개인 작업 브랜치.

---

### 2. 브랜치 보호 규칙 (Ruleset)

#### `main` 브랜치 규칙
> 메인 브랜치는 배포 전용으로, 가장 엄격한 기준을 적용합니다.

* **Pull Request 필수**: 모든 업데이트는 PR을 통해서만 가능합니다. (직접 `Push` 차단)
* **팀원 승인 필요 (Approval)**: 
  - 최소 **[4]명** 이상의 팀원이 리뷰하고 승인(Approve)해야 합칠 수 있습니다.
* **대화 해결 필수**: 코드 리뷰 시 달린 모든 의견(Conversation)이 **Resolved** 상태여야 합니다.
* **강제 푸시 방지 (Block Force Push)**: 기존 히스토리를 덮어쓰는 행위를 금지합니다.

#### `develop` 브랜치 규칙
> 개발 브랜치는 자유롭게 합치되, 팀 프로젝트의 최신 상태를 유지합니다.

* **기능 단위 머지**: 개인 브랜치 작업 완료 후 `develop`으로 합쳐 기능 검증을 진행합니다.
* **최신화 유지**: 작업 시작 전 항상 `main` 또는 `develop`으로부터 `Pull`을 받아 최신 상태를 유지합니다.

---

### Pull Request 가이드
1. 작업 완료 후 본인의 `feature/이름` 브랜치를 원격에 `Push` 합니다.
2. GitHub에서 `develop` 또는 `main`을 **Base**로 하여 PR을 생성합니다.
3. 팀원들의 리뷰를 기다리고, 수정 요청이 있을 경우 반영합니다.
4. 승인 요건이 충족되면 **Merge**를 진행합니다.

---
### PR 본문 양식(예시)
```
제목: [Release] v1.0.0 - 스파르타 7팀 자기소개 웹 서비스 완성

### 📦 작업 요약 (무엇을 했는지)
- `develop` 브랜치에서 검증된 모든 기능(마크다운 소개, 웹 확장 기능)을 `main`에 최종 반영
- 팀원별 개별 작업 브랜치 통합 완료 및 레이아웃 통일
- 프로젝트 초기 설정(`.gitignore`, `README.md`) 및 폴더 구조 정리

### 🎯 작업 의도 (왜 했는지)
- 팀 프로젝트의 마일스톤인 '자기소개 웹 페이지' 개발 완료에 따른 배포용 업데이트
- 개별 개발 결과물을 하나의 서비스 형태로 최종 병합하여 프로젝트 완성도 확보

### 🔍 확인 지점 (어디를 보면 되는지)
- `root`: 전체적인 프로젝트 구조 및 `README.md` 내의 협업 규칙 명시 확인
- `web/`: 팀원들의 HTML/CSS/JS 웹 페이지 작업물 통합 상태 확인
- `index.html`: 메인 페이지에서 각 팀원의 소개 페이지로 연결되는 링크 작동 여부

### 💬 리뷰 요청 포인트 (확인받고 싶은 부분)
- **병합 충돌 여부:** 모든 팀원의 코드가 충돌 없이 정상적으로 합쳐졌는지 최종 확인 부탁드립니다.
- **Ruleset 적용:** 현재 설정한 브랜치 보호 규칙에 따라 모든 팀원의 승인(Approve)이 필요합니다. 확인 후 승인 부탁드려요!
- **배포 확인:** 로컬 환경에서 실행 시 이미지 경로 인식에 문제가 없는지 마지막으로 체크해 주세요.

---
### 📸 완성된 페이지 스크린샷
(여기에 최종 결과물 화면을 드래그해서 넣어주세요!)
```

## 커밋 메시지 구조 및 규칙
### 구조
```
type : subject

body 

footer
```
---

### type rule
```
Type      설명	참고사항
Feat      기능 추가	기능 당 최초 한 번만 사용
Build     라이브러리 세팅/업데이트	라이브러리 설치 최초 한 번만 사용
Update    라이브러리 버전 업데이트/커스텀	 
Config	  Config 파일 설정	 
Remove	  파일 삭제	 
Fix	      기능 에러 수정	 
HotFix	  치명적인 버그 수정	main 브랜치에 바로 반영
Style     UI 관련 CSS 수정	 
Chore     단순 코드 수정	주석, 줄 바꿈, 임포트문, 세미콜론 등등..
Revise    기능 추가나 덧붙임을 위한 수정	 
Typo      오타 수정	 
Modify    기능 변경 있는 코드 개선/변경	 
Refactor  기능 변경 없는 코드 개선	 
Asset     이미지, 폰트	리소스 파일
Docs      문서 작성	 
```
---

### subject rule
- 제목은 최대 50글자 넘지 않는다.
- 마침표 및 특수기호를 사용하지 않는다.
- 영어로 작성 시 첫 글자는 대문자로 작성한다.
- 명령문을 사용한다.
- 간결하고 요점적으로 서술한다.

---

### body rule
- 한 줄당 72자 내로 작성한다.
- 상세하게 작성한다.
- 어떻게 보다는 무엇을, 왜 변경했는지 작성한다.

---

### footer rule
- 유형 : #이슈 번호의 형식으로 작성한다.
- 이슈 트래커 ID를 작성한다.
- 여러개의 이슈 번호는 ,로 구분
